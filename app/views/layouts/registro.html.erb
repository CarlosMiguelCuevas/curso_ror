<!DOCTYPE html>
<html lang="es">
<head>
  <%=render :partial => 'layouts/head'%>
</head>
<body>
  <!-- contenedor -->
  <div class="container">
    <!-- cabecera -->
    <%=render :partial => 'layouts/cabecera'%> 
    <!-- fin cabecera -->
    <!-- intro -->
    <%if hay_banner? then%>
      <%=yield :imagen%>
    <%else%>  
      <%=render :partial => 'layouts/intro'%>
    <%end%>
    <!-- fin de intro -->
    <!-- seccioes -->
    <!-- fin secciones -->
  </div>
  <!-- fin contenedor -->
  <div class="row">  
      <!-- es para llamar al contenido de la vista -->
      <%=yield%>
  </div>
  <!-- footer -->
  <%@arreglo = [1,2,"tres"]%>
  <%= render :partial => 'layouts/footer', :collect => @arreglo%>  
  <!-- end footer -->
                    
</body>
</html>
